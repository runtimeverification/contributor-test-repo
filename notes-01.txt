Adding some new documentation discussion notes. 

Forcing change to trigger WF triggering on main branch as target.

We need to now retrigger. The WF was a copy paste error from rv/k targeting the wrong API endpoint using rv/k not rv/contributor-test-repo
